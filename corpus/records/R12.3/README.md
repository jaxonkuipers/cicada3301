# R12.3 — telephone instruction to `845145127.com`

Calling R12.2's `(214) 390-9608` played: “Very good. You have done well. There are three prime numbers associated with the original final.jpg image. 3301 is one of them. You will have to find the other two. Multiply all three of these numbers together and add a .com to find the next step. Good luck. Goodbye.” R12.1's image dimensions supplied the other primes, so `509 × 503 × 3301 = 845145127` and the destination was `http://845145127.com/`.

The domain served `cicada.jpg`. OutGuess returned a signed [“Patience is a virtue” message](artifacts/communications/2012-01-patience-check-back.asc) directing solvers to return at `17:00 UTC, Monday 9 January 2012`. The domain, carrier and time gate pass to [R12.4](../R12.4/).

The telephone service and authoritative recording are gone. Surviving recordings and transcriptions agree on the instruction, and its explicit arithmetic fixes the historical construction of `845145127.com`.
