# R12.5 — individualized RSA, MIDI and signed solver return

**Solver state.** An enrolled solver possessed a fresh email address, a received number and a private same-URL path on the enrollment onion.

**Dependencies consumed.** R12.4 supplies the enrollment onion, fresh email address, received number and private same-URL path.

**Artifact and action.** Recipient-preserved material describes a one-display page that supplied a unique low-bit RSA modulus and unique `Crypt::RSA` 1.99 OAEP ciphertext to each solver. The retained instance measurements use `e=65537`, a 372-bit modulus and four 47-byte ciphertext blocks. Factoring the modulus and reproducing the legacy OAEP convention yields the exact 20-digit plaintext `33521494043430258676`; the solver returned that value to the same URL. A correct response reportedly produced “Correct. We'll email you.” Sharing individualized material triggered the locally preserved authenticated removal message in [`2012-01-sharing-disqualification.asc`](../2012/communications/2012-01-sharing-disqualification.asc).

**Resulting state.** Recipient reconstruction describes the next stage as a signed and encrypted MIDI carrier. Its first track's notes and delta times supplied the alphabet that decoded the instruction: create a GPG key for the solver's email; upload it to the MIT keyservers; encrypt a supplied word list to Cicada's public key; sign it with the solver's key; send the ASCII-armored ciphertext to the Gmail address from which the numbers arrived. The second track used the Blake chorus printed in the accompanying message as its known answer.

**State handed forward.** The reconstructed return instructions and authenticated anti-sharing enforcement survive, while recipient assessment and selection state do not. R12.7 separately records the authenticated public close.

**Evidence.** The RSA measurements and decoded MIDI instructions survive in a redacted [recipient transcript](https://github.com/krisyotam/cicada3301/blob/main/archives/cijhho/2012/MIDI%20puzzle/MIDI%20Puzzle.txt) and later reconstruction. The exact signed bytes locally preserved from this private stage are the anti-sharing notice under [`corpus/2012/communications/`](../2012/communications/). The public round closes separately at [`R12.7`](R12.7-public-close.md).

**Later evidence.** Cicada used recipient-specific ciphertext, one-display callback state, a solver-generated public key, solver signatures, encrypted email and explicit anti-collaboration enforcement in 2012. These roles define the demonstrated 2012 system design.

**Preservation boundary.** The exact RSA page, MIDI carrier, post-MIDI return packet, recipient mailbox, server implementation and private reply after the signed word-list submission are absent from the local corpus. A leaked February recruitment letter describes Cicada's stated beliefs and organization, but its public paste is unauthenticated and cannot supply route state. The public round closed with the signed message recorded at R12.7 on 6 February.
