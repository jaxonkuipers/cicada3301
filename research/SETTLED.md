# Established state

This file contains compact facts required to interpret the preserved route and corpus. New cross-campaign conclusions belong here only after their evidence is committed under `research/attacks/`.

## Authenticated route

- Cicada's OpenPGP fingerprint is `6D85 4CD7 9333 22A6 01C3 286D 181F 01E5 7A35 090F`.
- The reproduced public route ends with the April 2014 onion-7 delivery of the 58 Liber Primus body images.
- The January 2016 signed message identifies Liber Primus as the way and assigns roles to its words, meaning and numbers.
- The April 2017 signed warning is the latest authenticated public Cicada communication in the corpus.
- The successful 2014 callback's submitted values, private key, HTTP exchange and recipient state are absent from the preserved corpus.

## Corpus invariants

- Liber Primus contains 15 indexed sections across 75 images: 17 introduction images and 58 body images.
- Eight sections remain unsolved: `0.5` through `0.12`, totaling 12,956 runes.
- The accepted solved text contains 2,901 runes and is reproduced exactly by `python3 -m lib.corpus`.
- Typed punctuation and illustrations are represented separately from the rune stream.

## Demonstrated book operations

- Section `0.0` decrypts by inverted Gematria Primus.
- Section `0.1` uses cyclic `DIVINITY`; plaintext F passes through while holding the key, and its closing instruction is plaintext.
- Section `0.2` uses inverted Gematria followed by a shift of three.
- Sections `0.3` and `0.14` are plaintext through Gematria Primus spelling.
- Section `0.4` uses cyclic `FIRFUMFERENFE` with the same plaintext-F behavior, followed by a plaintext instruction.
- Section `0.13` uses the Euler totients of consecutive primes with one plaintext-F hold.

## Preserved unresolved objects

- The section labels and boundaries are corpus indexing conventions; no authenticated instruction assigns one cipher per section.
- Pages 49–51 contain one contiguous run of 256 two-character base-60 groups that decode uniquely to 256 bytes.
- Pages 56–57 state that a deep-web page hashes to the printed 512-bit value.
- Page 15 contains a red-start Fibonacci-indexed prime spiral; its intended role is unresolved.
- Introduction pages 05 and 16 contain 5×5 number/word squares associated with 1033 and 3301.
