__pycache__/
*.py[cod]

# SQLite sidecars are transient; discord.db itself is preserved in Git.
discord.db-wal
discord.db-shm

.ruff_cache/
.venv/

# Campaign findings and scripts are committed; generated output stays local.
research/campaigns/*/out/
research/explog/.lock

# Same convention at the repo root; peers write caches to ./out/.
/out/
